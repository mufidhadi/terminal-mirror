[project]
name = "terminal-mirror"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=50.0.1",
    "msgpack>=1.2.2",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "websockets>=17.1",
]
